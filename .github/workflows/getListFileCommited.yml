# This is a basic workflow to help you get started with Actions

name: CI-List-File

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    environment:
      name: Development
      url: 'https://iicssaas.requestcatcher.com/'
      
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run get list file commited
        run: git diff-tree --no-commit-id --name-only -r ${{ github.sha }}
        
      # Runs a set of commands using the runners shell
      - name: Run get list file commited
        run: echo Last Commited Hash ${{ github.sha }}

      - name: Login IICS Development
        id: myRequest
        uses: fjogeleit/http-request-action@v1
        with:
          url: 'https://dm-us.informaticacloud.com/saas/public/core/v3/login'
          method: 'POST'
          customHeaders: '{"Content-Type":"application/json","Accept":"application/json"}'
          data: '{"username": "${{ secrets.IICS_USER }}","password": "${{ secrets.IICS_PASSWORD }}"}'
          
      - name: Show Response
        id: session-id
        run: |
            echo ${{ steps.myRequest.outputs.response }}
            echo Session-Id: ${{ fromJson(steps.myRequest.outputs.response).userInfo['sessionId'] }}
            echo '::set-output name=SESSION_ID::${{ fromJson(steps.myRequest.outputs.response).userInfo['sessionId'] }}'
            
      - name: Send pullByCommitHash IICS Development
        id: myRequest1
        uses: fjogeleit/http-request-action@v1
        with:
          url: 'https://iicssaas.requestcatcher.com/public/core/v3/pullByCommitHash'
          method: 'POST'
          customHeaders: '{"Content-Type":"application/json","Accept":"application/json","INFA-SESSION-ID":"132131321321321"}'
          data: '{"Session-Id": ${{ steps.session-id.outputs.SESSION_ID }},"commitHash": "${{ github.sha }}" }'

